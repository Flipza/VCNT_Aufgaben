apiVersion: batch/v1
kind: Job
metadata:
  name: urlclicker
spec:
  template:
    spec:
      containers:
      - name: urlclicker
        image: alpine
        imagePullPolicy: IfNotPresent
        command: 
        - echo Starting Backup...
      restartPolicy: Never
